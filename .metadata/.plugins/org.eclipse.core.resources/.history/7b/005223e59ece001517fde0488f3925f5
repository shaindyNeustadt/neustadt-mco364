package neustadt.mco364.colorku;

import java.awt.BorderLayout;
import java.awt.Color;
import java.awt.GridLayout;

import javax.swing.BorderFactory;
import javax.swing.JLabel;
import javax.swing.JPanel;


public class SudokuPanel1 extends JPanel{

	private Board board;
	private JPanel[][] panels;
	private Field[][] fields;
	
	public SudokuPanel1(Board board) {
		setLayout(new GridLayout(3,3));
		this.board = board;
		this.panels = new JPanel[3][3];
		for(int x = 0; x < 3; x++){
			for(int y = 0; y < 3; y++){
				panels[x][y] = new JPanel();
				panels[x][y].setBorder(BorderFactory.createLineBorder(Color.RED));
				add(panels[x][y]);	
			}
		}
	fields = new Field[9][9];	
	for(int x =0; x < 9; x++){
		for(int y = 0; y < 9; y++){
			
		}
	}
	}	
}